main() {
    x = @alloc_heap(2);
    x[0] = -122;
    y = @alloc_heap(1);
    x[1] = 1;
    y[0] = 987;
    z = @alloc_heap(1);
    z[0] = -567;
    if 1 {
        @spawn(f1, x);
    }
    f0(y);
    loop {
        @spawn(f0, z);
        break 0;
    }
    return;
}

println_i32(x i32) {
    @print_i32(x);
    @print_char(10);
    return;
}

f0(x addr) {
    println_i32((x[0]));
    return;
}

f1(x addr) {
    println_i32(((x[0]) - (x[1])));
    return;
}
